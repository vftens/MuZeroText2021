"use strict";
exports.__esModule = true;
exports.updateAt = exports.sortBy = void 0;
var tslib_1 = require("tslib");
exports.sortBy = function (array, param, order) {
    if (order === void 0) { order = 'asc'; }
    return tslib_1.__spreadArrays(array).sort(function (a, b) { return (order === 'desc' ? -1 : 1) * (a[param] < b[param] ? -1 : 1); });
};
exports.updateAt = function (array, index, newItem) {
    return index >= array.length
        ? array
        : tslib_1.__spreadArrays(array.slice(0, index), [newItem], array.slice(index + 1, array.length));
};
